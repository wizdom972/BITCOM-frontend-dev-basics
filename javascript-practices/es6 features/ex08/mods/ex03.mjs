/*
    Named Export Module
    
    1. exports(require)와 비슷하다.
    2. import하는 모듈에 destructing 할 수 있다.
    3. export할 때 이름을 지정할 수 있다.
    4. export할 때 이름을 변경할 수 있다.
    6. 다중 export가 가능하다.

*/

export const add = (a, b) => a + b;
export const sub = (a, b) => a - b;
